#include <stdio.h> // 1. Load the screen-printing tool

int main() { // 2. Open the main gate of the program
    
    int age = 10; // 3. Create a box for a number, put 10 inside
    
    if (age >= 18) { // 4. Check a condition
        printf("You can drive!"); 
    } else {
        printf("Too young!"); // 5. Print text to the screen
    }
    
    return 0; // 6. Tell the computer everything finished perfectly
} // 7. Close the gate
